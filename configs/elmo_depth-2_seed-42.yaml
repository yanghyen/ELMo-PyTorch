# ELMo 모델 설정 (Depth 2)
vocab_size: 30000
embedding_dim: 512
hidden_dim: 512
num_layers: 2  # depth: 2
dropout: 0.1
seq_len: 20  # 시퀀스 길이
batch_size: 32
lr: 0.001
epochs: 1
seed: 42
# Subsampling 설정
enable_subsampling: true
subsample_t: 1e-3
num_workers: 16


